name: memory-recommendation

on:
  push:
    branches: [main]
    paths:
      - "src/aios_core/query/search.py"
      - "src/aios_core/recommendation/**"
      - "tests/integration/test_v3_memory_recommendation.py"
      - ".github/workflows/memory-recommendation.yml"
  pull_request:
    paths:
      - "src/aios_core/query/search.py"
      - "src/aios_core/recommendation/**"
      - "tests/integration/test_v3_memory_recommendation.py"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - run: pytest tests/integration/test_v3_memory_recommendation.py tests/integration/test_m0_prime_store_delta_and_search.py

# CORE-RC-FREEZE-001 transient probe trigger; no command or test semantics changed.
