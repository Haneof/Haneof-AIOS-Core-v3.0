name: C14 R5 Cleanup

on:
  push:
    branches:
      - c14/sem-res-sol-r5-b81e37
    paths:
      - .github/workflows/c14-resident-repair-isolated-r5.yml

concurrency:
  group: c14-sem-repair-resident-isolated-r5-b81e37
  cancel-in-progress: true

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by isolated r6; cancel prior run."
