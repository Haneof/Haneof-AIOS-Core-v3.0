name: cognition-writeback

on:
  push:
    branches: [main]
    paths:
      - "src/aios_core/writeback/**"
      - "src/aios_core/contracts/**"
      - "src/aios_core/dependency/**"
      - "tests/integration/test_v3_cognition_writeback.py"
      - ".github/workflows/cognition-writeback.yml"
  pull_request:
    paths:
      - "src/aios_core/writeback/**"
      - "tests/integration/test_v3_cognition_writeback.py"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - run: pytest tests/integration/test_v3_cognition_writeback.py
