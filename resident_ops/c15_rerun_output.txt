usage: mechanical_ingest_adapter.py [-h] --world-db WORLD_DB
                                    [--event-file EVENT_FILE]

Mechanically ingest exactly one already-released C14 fixture event.

options:
  -h, --help            show this help message and exit
  --world-db WORLD_DB
  --event-file EVENT_FILE
                        JSON file containing only the released event
                        projection; '-' reads stdin.
usage: release_operator.py ack [-h] --phase {A,B,C} --state STATE --world-db
                               WORLD_DB --sequence SEQUENCE --event-id
                               EVENT_ID --ingest-ref INGEST_REF
                               [--conversation-session-id CONVERSATION_SESSION_ID]
                               [--conversation-turn-index CONVERSATION_TURN_INDEX]

options:
  -h, --help            show this help message and exit
  --phase {A,B,C}
  --state STATE
  --world-db WORLD_DB
  --sequence SEQUENCE
  --event-id EVENT_ID
  --ingest-ref INGEST_REF
  --conversation-session-id CONVERSATION_SESSION_ID
  --conversation-turn-index CONVERSATION_TURN_INDEX
