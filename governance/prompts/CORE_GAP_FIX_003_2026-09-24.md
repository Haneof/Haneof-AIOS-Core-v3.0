# CORE-GAP-FIX-003 — User Turn IN_DOUBT Reconciliation

Repository: `Haneof/Haneof-AIOS-Core-v3.0`

Role: Core Turn-Recovery Engineer

Single task: close only `CG-003 USER_TURN_IN_DOUBT_RECOVERY`.

Proceed only when task board says `CORE-GAP-FIX-003 = READY`, after FIX-002 is independently accepted and merged.

## Before-fix reproduction

On current main prove that an exact user turn left in `started` after interruption is permanently refused as `TurnExecutionInDoubt` for the same identity and there is no supported inspection/reconciliation transition other than abandoning the turn or editing storage out of band.

## Required semantics

Keep the A09 at-most-once fail-closed default, but add a supported durable inspection/reconciliation API for the exact turn identity.

It must:
- inspect exact turn execution state plus durable assistant output, model-attempt/provenance and metering receipts;
- resolve definitely completed cases without re-inference;
- preserve `UNKNOWN/IN_DOUBT` where proof is insufficient;
- allow retry only when durable evidence establishes that model execution was definitely not submitted/executed;
- reject conflicting input for the same turn identity;
- never require clearing the execution table or changing `turn_id/session_id/turn_index` as a bypass.

Reuse the model-attempt mechanism integrated by FIX-002; do not create a second recovery ledger.

## Tests

At minimum:
- pre-model failure proven not submitted -> authorized retry;
- ambiguous provider interruption -> remains IN_DOUBT, no model reinvocation;
- provider/model return known but assistant write interrupted -> no blind second model call;
- durable assistant output -> reconcile completed;
- already completed replay -> no reinference;
- conflicting input -> fail closed;
- restart across each state.

Run affected runtime/turn execution suites and full repository regression required by governance.

No Resident execution. No historical evidence rewrite. No UI/hardware/ROM.

Open a focused PR and stop at `REVIEW_READY`. Independent reviewer required. Do not merge or begin Headless.
