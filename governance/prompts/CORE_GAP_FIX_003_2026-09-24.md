# CORE-GAP-FIX-003 — User Turn IN_DOUBT Recovery

Repository: Haneof/Haneof-AIOS-Core-v3.0

Status at dispatch creation: BLOCKED on independent acceptance/integration of CORE-GAP-FIX-002.

Role when released: Core Runtime / Recovery Engineer.

Do not execute this task while BLOCKED.

## Task after release

Fix only CG-003 USER_TURN_IN_DOUBT_RECOVERY from reviews/CORE_GAP_AUDIT_001_2026-09-24.md.

Start from then-current main after CORE-GAP-FIX-002 integration. Reuse accepted execution-attempt/reconciliation primitives where semantically appropriate; do not build a parallel second recovery framework.

Required behavior:
- preserve A09 fail-closed at-most-once admission;
- provide supported inspection/reconciliation for a started user turn;
- distinguish provably completed, provably not executed/retryable, and UNKNOWN/IN_DOUBT;
- allow retry only with durable proof of non-execution and explicit authorized transition;
- never clear the table, mutate history, swap turn_id, or silently reinvoke a possibly executed model call;
- recover completed output when durable receipts prove completion;
- conflicting input for the same turn identity remains fail-closed.

Required tests:
- pre-model known failure;
- ambiguous provider interruption;
- provider response/provenance durable but later write interrupted;
- assistant-output persistence failure;
- completed-output recovery;
- conflicting input;
- restart/reopen behavior;
- compatibility with accepted background model-attempt semantics from CORE-GAP-FIX-002.

Deliverable: focused PR, code/tests/evidence, author state REVIEW_READY, separate independent acceptance required.
