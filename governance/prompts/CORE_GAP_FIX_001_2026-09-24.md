# CORE-GAP-FIX-001 — Runtime Temporal Read Cut

Repository: `Haneof/Haneof-AIOS-Core-v3.0`

Role: Core Runtime Engineer

You are not the PM, not a Resident, not the independent acceptance reviewer, and not an operator engineer.

## Single task

Close only `CG-001 RUNTIME_TEMPORAL_READ_CUT` from `reviews/CORE_GAP_AUDIT_001_2026-09-24.md`.

Start by fetching real-time latest `main`. Read:
- `governance/AIOS_SINGLE_WINDOW_TASK_BOARD.md`
- `AIOS_v3.0_CURRENT_CHECKPOINT.md`
- `governance/CORE_GAP_AUDIT_001_PM_DISPATCH_2026-09-24.md`
- `reviews/CORE_GAP_AUDIT_001_2026-09-24.md`
- `governance/AIOS_CORE_COMPLETION_PLAN_2026-09-24.md`

Proceed only if `CORE-GAP-FIX-001 = READY`.

## Required before-fix reproduction

Prove on current main that an execution whose cognition/write time is pinned to T1 can, after a later T2 fact enters World, expose that T2 fact through one or more Resident model-visible read capabilities and then write at T1.

At minimum cover:
- `search_world`;
- `inspect_world_object`;
- resumed C14 cognitive derivation;
- resumed Periodic Review.

Also inspect other model-visible World/AI-world read capabilities for the same temporal-cut bug. Do not declare them safe without checking.

Save the reproduction in targeted tests/evidence before changing implementation.

## Required semantics

Define one explicit runtime read cutoff for a time-pinned execution.

When a runtime execution is intentionally pinned to an earlier cognition/write time:
- model-visible reads must not reveal World knowledge whose `learned_at` is after the cutoff;
- semantic search must use existing AS_KNOWN semantics where possible;
- exact object inspection must fail closed if the requested revision was not yet known at the cutoff;
- AI-world/current cognition reads and other relevant model-visible reads must obey the same cutoff;
- no future fact may be written back as if learned at the earlier time.

Do not solve this only in the operator. This is a Core runtime read-consistency fix.

## Constraints

Do not redesign World, Summary, Index, or the operator.
Do not patch historical Resident evidence.
Do not change UI/hardware/Android code.
Do not run Resident A/B/C.
Do not weaken current subject isolation, evidence pinning, or current-view semantics.

Prefer extending/reusing existing `AS_KNOWN` machinery over creating another temporal truth store.

## Regression requirements

Add targeted regressions for:
- search at historical cutoff;
- exact-object drill-down at historical cutoff;
- resumed C14 with later fact present;
- resumed Periodic Review with later fact present;
- current-time ordinary execution still sees eligible current knowledge;
- missing/corrupt `learned_at` fails closed where historical visibility is requested.

Run targeted tests, affected runtime/search/summary/review suites, and full repository regression required by current governance.

## Delivery

Open a focused PR from latest main. Record:
- construction main SHA;
- before-fix reproduction;
- exact changed files;
- test commands/results;
- CI run/job IDs;
- `NO RESIDENT EXECUTED`;
- any semantics not addressed.

Final author state: `REVIEW_READY`.

Do not self-approve, merge, start FIX-002, or mark DONE.
