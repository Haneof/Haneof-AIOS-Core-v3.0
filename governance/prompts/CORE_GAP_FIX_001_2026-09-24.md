# CORE-GAP-FIX-001 — Runtime Temporal Read Cut

Repository: Haneof/Haneof-AIOS-Core-v3.0

Role: Core Runtime Engineer.

You are not the PM, not the audit author, not the independent acceptance reviewer, not a Resident, and not a semantic evaluator.

## Task

Fix only CG-001 RUNTIME_TEMPORAL_READ_CUT from the accepted audit report reviews/CORE_GAP_AUDIT_001_2026-09-24.md.

Before coding:
1. Fetch live main.
2. Read governance/AIOS_SINGLE_WINDOW_TASK_BOARD.md, AIOS_v3.0_CURRENT_CHECKPOINT.md, governance/AIOS_CORE_GAP_DISPATCH_2026-09-24.md, governance/AIOS_CORE_COMPLETION_PLAN_2026-09-24.md, and the accepted audit report.
3. Confirm CORE-GAP-FIX-001 is READY. If not READY, stop.

## Required reproduction

Prove on live main that an execution whose write/cognition time is pinned to T1 can resume after a T2 fact is learned and model-visible reads can expose T2 data while the resulting cognition is still written as T1.

Cover at minimum:
- resumed C14 cognitive derivation;
- resumed Periodic Review;
- model-visible search_world;
- exact inspect_world_object / drill-down;
- a normal current-time execution control case that must continue seeing current facts.

Do not rely only on source inspection. Create a targeted executable regression that fails before the fix.

## Minimal implementation

Define one explicit active runtime read cutoff for time-pinned execution and apply it consistently to every relevant model-visible World/cognition read path.

Requirements:
- reuse existing AS_KNOWN semantics in query/search.py where possible;
- search_world must not return objects learned after the active cut;
- inspect_world_object and related exact-object reads must fail closed / return unavailable when the object or requested revision was not yet knowable at the cut;
- AI-world/current cognition reads exposed to the model must obey the same cut;
- ordinary current-time turns must preserve existing behavior;
- do not redesign World, Summary, Index, operator, fixture, or Resident semantics;
- preserve exact revision/provenance behavior;
- no historical evidence rewriting.

## Required tests

At minimum add targeted regressions for:
- resumed C14 with T2 late fact excluded;
- resumed Periodic Review with T2 late fact excluded;
- search_world at historical cut;
- inspect_world_object at historical cut;
- current-time control path;
- missing/corrupt learned_at fail-closed behavior if relevant to the touched path.

Run targeted tests plus all directly affected runtime/query/summary/review tests and the repository full regression gate required by current governance.

## Deliverable

Create a focused PR containing code + tests + completion evidence.
The PR must state construction main SHA, before-fix reproduction, exact files changed, test commands/results, and whether any experiment-impact ruling changes.

Because this changes Core semantics, explicitly state that historical Resident evidence is not patched and that fresh A/B/C remains required only after future RC freeze.

Final author state: REVIEW_READY.
Do not merge your own PR. Do not start CORE-GAP-FIX-002/003.
