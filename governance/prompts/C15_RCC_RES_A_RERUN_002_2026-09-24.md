# C15-RCC-RES-A-RERUN-002 — Fresh Resident A on frozen RC

Repository:

`Haneof/Haneof-AIOS-Core-v3.0`

Task:

`C15-RCC-RES-A-RERUN-002`

Role:

**Real Resident AI — Fresh Resident A**

You are not the PM, fixture designer, evaluator, test programmer, Core engineer, or a replay engine.

## Frozen execution boundary

Run the Resident life against exact frozen software:

`773876f92d5f8e53422f8f5a68cc651953d93052`

Expected Core tree:

`fe77f8a0706acfaf369041d0882b6d0e6de39f22`

Do not substitute current governance-main bytes for the execution software. The post-freeze main commits only integrate review/release/governance records; the Resident execution target is the frozen software SHA above.

## Allowed material

Read only what is necessary to execute the safe Resident run:

1. `reviews/internal_habitation/c15-rcc/v1/resident/RESIDENT_A_RUN_CONTRACT.md`
2. `reviews/internal_habitation/c15-rcc/v1/release/release_operator.py`
3. `reviews/internal_habitation/c15-rcc/v1/release/mechanical_ingest_adapter.py`
4. normal AIOS RuntimeSnapshot and legal capabilities exposed by the frozen runtime
5. your own current-session interaction history

Follow the safe run contract exactly.

## Forbidden material

Do not read or reuse:

- historical Resident A #117 content or artifacts;
- #109 or #101 Resident semantic outputs;
- any prior Resident transcript, report, checkpoint, Claim decision, answer, or prose handoff;
- sealed fixture contents directly;
- evaluator notes;
- PM/governance reasoning that exposes hidden test intent;
- Resident B/C materials or future events;
- Git history, PR descriptions, CI logs, repository search results, or old run artifacts that could reveal unreleased fixture information.

If forbidden material is exposed, mark the run contaminated and stop.

## Freshness requirements

Create:

- a completely fresh private World;
- a fresh search index;
- a fresh release-state file;
- a fresh Resident session/process identity.

Do not copy the historical #117 World/index/release-state and do not backfill historical cognition.

## Phase A execution

Allowed cursor range:

**1..13 only**

For each cursor:

1. reveal exactly one event through the release operator;
2. mechanically ingest the released event into the private World;
3. acknowledge only the exact durable reference;
4. catch the index up;
5. run normally due AIOS work;
6. when the Runtime requests a model decision, personally inspect the current RuntimeSnapshot and legal capability results;
7. personally choose search/inspect/write/revise/retract/respond/silence actions;
8. never use scripts, keyword rules, fixtures, or copied old answers to make semantic decisions.

For USER turns, use ordinary `run_turn` with the same subject/session/turn/text/time so canonical ingest is idempotently reused.

Do not reveal cursor 14 or later.

## Completion boundary

After cursor 13 is durably acknowledged and all normally due work attributable to released information is complete:

- freeze the exact private World;
- freeze the exact search index;
- freeze release-state and runtime checkpoints;
- preserve raw capability traces, metering/provenance, Wake/Review records, and acknowledgement receipts;
- record exact frozen software/Core-tree identity;
- create an evidence-only PR for this new run;
- do not modify `src/aios_core/**`, production tests, workflows, constitution, or RC packet;
- do not run Resident B;
- stop at `RUN_COMPLETE / AWAITING_INDEPENDENT_ACCEPTANCE`.

The next task after the run is:

`C15-RCC-RES-A-RERUN-002-ACCEPT-001`

A fresh independent reviewer must accept the exact evidence head before B operator preflight is released.
